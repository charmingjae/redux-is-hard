import { Button } from '@mui/material';
import React from 'react';

const TButton = ({ variant, type }) => (
	<Button variant={variant} type={type}>
		Hello World
	</Button>
);

export default TButton;
